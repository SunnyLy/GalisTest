<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.galis.galistest" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".AppStartActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".tag.TagActivity" />
        <activity android:name=".draw.CalTimeActivity" />
        <activity android:name=".Matrix.MatrixActivity" />
        <activity android:name=".Scroll.ScrollActivity" />
        <activity android:name=".Scroll.PullScrollActivity" />
        <activity android:name=".draw.CanvasDraw" />
    </application>

</manifest>
